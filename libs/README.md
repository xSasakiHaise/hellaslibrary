# HellasControl Dependency

Place the HellasControl v2.0.0 JAR in this directory and ensure it is named `hellascontrol-2.0.0.jar` so Gradle can resolve the compileOnly dependency declared in `build.gradle`.

